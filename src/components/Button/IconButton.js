import React from 'react';
import styled from 'styled-components';

const IconButtonElement = styled.default.button`

`;

export default function IconButton() {
  return (
    <IconButtonElement>

    </IconButtonElement>
  );
}
